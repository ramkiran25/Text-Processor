import { Routes } from '@angular/router';
import { TextProcessorComponent } from '../text-processor-component/text-processor.component';

export const routes: Routes = [{ path: '', component: TextProcessorComponent }];
